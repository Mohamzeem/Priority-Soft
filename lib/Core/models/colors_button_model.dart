import 'package:flutter/material.dart';

class ColorButtonModel {
  String title;
  double width;
  Color circleColor;
  ColorButtonModel({
    required this.title,
    required this.width,
    required this.circleColor,
  });
}
